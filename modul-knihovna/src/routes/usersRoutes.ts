// import express from 'express';
// import * as usersController from '../controllers/usersController';
// import { validateRequest } from '../middleware/validation';
// import { userCreateValidator, userUpdateValidator } from '../middleware/validators/usersValidator';

// const router = express.Router();

// router.get('/', usersController.getUsers);
// router.get('/:id', usersController.getUserById);
// router.post('/', validateRequest(userCreateValidator), usersController.createUser);
// router.put('/:id', validateRequest(userUpdateValidator), usersController.updateUser);
// router.delete('/:id', usersController.deleteUser);

// export default router;
